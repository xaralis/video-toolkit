# Video Toolkit Python Dependencies
elevenlabs>=1.0.0
python-dotenv>=1.0.0
requests>=2.28.0
boto3>=1.28.0  # For Cloudflare R2 (S3-compatible)

# Image processing — used by video_toolkit/flux2.py, video_toolkit/image_edit.py,
# and the moviepy examples for PIL-based text rendering
Pillow>=10.0

# Array math — transitive dep of moviepy, pinned here because
# some image processing examples import it directly.
numpy>=1.24

# moviepy examples (examples/quick-spot, examples/data-viz-chart) —
# Python-native video composition as an alternative to Remotion for
# short-form ad spots, lower thirds, and data-driven scenes
moviepy>=2.0
matplotlib>=3.7

# Beat detection (video_toolkit/detect_beats.py) — tempo/beat/downbeat
# extraction from an audio track via onset-strength + dynamic programming.
# Floor is 0.11, not 0.10: 0.11 is the first release that supports NumPy 2 and
# Python 3.13+, and the first to declare the `standard-sunau` shim (stdlib sunau,
# which librosa's audioread backend imports, was removed in 3.13). On a >=3.13
# interpreter an 0.10.x resolution would install without that shim and crash at
# `import librosa`. The floor must track requires-python (>=3.13).
librosa>=0.11

# Brand linting (check_brand.py) and SRT export (export_srt.py) parse
# Root.tsx-adjacent config that contains trailing commas, which json can't read
json5>=0.9
